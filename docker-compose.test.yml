services:
  backend:
    environment:
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      APP_COOKIE_SECURE: "false"
      APP_COOKIE_SAME_SITE: "Lax"

  frontend:
    ports:
      - "3000:3000"

  cms:
    ports:
      - "3001:3000"
